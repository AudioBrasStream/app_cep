// list_screen.dart
import 'package:flutter/material.dart';

class ListScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("CEPs Cadastrados")),
      body: Center(
        // Implemente a listagem de CEPs do Back4App aqui
      ),
    );
  }
}